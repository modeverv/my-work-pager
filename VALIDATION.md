# Work Pager v0.1 検証記録

実施日: 2026-09-11（JST）
環境: Apple Silicon / macOS 26.6.1 / Xcode 26.6

## 実施済み

| 項目 | 結果・証拠 |
|---|---|
| ネイティブアプリ | Swift / SwiftUI / AUHAL / AVFoundation / Vision。Releaseビルド成功、通常ウインドウで実機起動 |
| 署名 | ローカルad-hoc署名。`codesign --verify --strict`成功 |
| 自動テスト | 実学習音を使う任意テストを含め12件成功、失敗0件（22:07 JST） |
| RME入力 | Fireface UCX II (24100500)を選択し、Mic/Line 1の実入力レベル約−59 dBFSを表示 |
| 多チャンネル | Core Audioの20入力チャンネル名を列挙。20番のADAT 8を選択して入力処理開始成功 |
| 既定入力 | テスト開始時・終了時ともSOUNDPEATS Space。Work Pagerによる既定入力の変更なし |
| Learn | Slack設定のKnock BrushをBlackHole 2ch / Input 1経由で学習。約0.51秒、ローカル保存成功 |
| 保存・再起動 | 学習テンプレート、デバイス、Auto設定、秘密topicが再起動後も保持。起動直後はDISARM |
| Live検出 | SlackのKnock Brushを繰り返し再生し、0.973 / 1.000 / 1.000で検出。最終ビルドでも22:07:15に1.000で検出 |
| 別音 | SlackのDingはスコア0.000で通知なし |
| オフライン別音 | macOSの14種のシステム音は全て0.90未満。最大0.0225（Glass）。実環境の全音声に対する誤検出率を保証する試験ではない |
| Cooldown | 21:58:06の検出後、30秒以内の同音再生はMatch 1.000になっても最終検出時刻を更新せず通知抑止。その後21:58:56は再通知 |
| ntfyテスト | UIのテストボタンでHTTP 200。iPhoneへの到達をユーザーが確認 |
| ntfy音声検出 | Slack音の実検出でHTTP 200。テスト通知と合わせて「2件とも届いた」とユーザー確認 |
| ネットワーク障害 | 一時的な`.invalid`サーバーでエラー−1003を表示し、アプリは継続動作。`https://ntfy.sh`へ復元 |
| カメラ | HP HD CameraでPRESENT / ABSENTの表示を確認。約2回/秒のVision人物検出 |
| 在席→DISARM | 在席5秒の設定で、自動DISARMと音声入力停止を確認 |
| 不在状態 | ユーザー離席時にABSENT 43秒・66秒・76秒とARM状態を確認。30秒の境界判定は自動テストでも検証 |
| Override | Auto中に手動ARMし、在席33秒以上でもARMが維持されることを確認 |
| Resume Auto | Resume後の在席カウンターが0に戻り、その後自動DISARMすることを確認 |
| カメラなし | 未選択時にARM状態を保ってManualへ移行。エラー理由を保持するようUIを修正し、「カメラが見つかりません。手動操作に切り替えました。」の表示も実機確認 |
| データ境界 | 通信APIに渡す引数はサーバーとtopicだけ。本文・タイトルは固定。音声・カメラフレームをネットワーク処理へ渡さないことをコード確認 |

## 本番利用前に残る確認

会社PCは現在使えないとのユーザー回答により、会社PC → RME ADAT 8の実通知音を使うLearnと再検出は未実施です。現在の学習音は開発用BlackHole経由のものです。

会社PCを使えるときに、ADAT 8のレベル確認 → Learn → 実Slack通知音 → ARMして再生 → iPhone受信確認を行ってください。必要ならしきい値を調整してください。会社PCの音量、DAC/ADC、サンプルレートが違う実経路での検証結果はまだありません。

カメラの権限拒否・物理抜線は実機では再現していません。拒否・未接続・フレーム停止のハンドリングは実装しています。長時間稼働、スリープ復帰、多種類の業務音声による長時間誤検出率評価は今後の確認です。

## 終了時の設定

- Work Pager入力: Fireface UCX II (24100500) / **20: ADAT 8**
- 通知音: 開発用Slack Knock Brush、約0.51秒（会社PCで再Learnが必要）
- ARM状態: **Manual / DISARMED**（本番でLearn後にAutoを選択可能）
- カメラ: HP HD Camera
- 検出しきい値0.90 / cooldown 30秒 / 不在30秒 / 在席5秒
- ntfy: `https://ntfy.sh`、topicはKeychain。秘密の値はこの記録やソースに含めない
- macOSの出力・サウンドエフェクト出力: どちらも元のSOUNDPEATS Spaceへ復元
- Slack通知音設定: 元のKnock Brushへ復元

## 実装上の検証メモ

Computer UseのネイティブサービスがSwiftUI GroupBoxのアクセシビリティ解析時にクラッシュしたため、画面を通常のVStack構成に変更して解消しました。アプリ本体のクラッシュではありません。

入力変更・DISARM時に古い音声処理結果が通知されないよう、キャプチャ世代をチェックしています。リアルタイムコールバックは事前確保バッファを使用し、ロック待ちをせず、欠落時には候補を破棄します。カメラのサンプル間隔が途切れた場合はpresence継続時間をリセットします。

## 追加: カメラ検出デバッグ窓

2026-09-11: Auto Cameraの在席原因を確認する別窓を追加。HP HD Cameraで最新フレームが届き、「検出1人 / 在席判定の対象1人」と状態・継続時間が更新されることを実機のアクセシビリティ情報で確認。人物の枠はVisionと同じ未反転フレームに重ね、信頼度50%以上を緑、未満を黄色で表示する実装。家族のカメラ映像のスクリーンショットは取得・保存していない。

Releaseビルド・署名検証成功。既存の通常テスト11件成功（ローカル学習音テストは環境変数未指定でスキップ）。Autoの人物判定条件・ARM遅延・音声経路・ntfy送信内容は変更していない。

## 追加: Dockアイコン更新

2026-09-11: アイコン組み込み後もmacOSのアプリ登録情報が標準アイコンを返す状態を確認。バンドルをtouchしてLaunch Servicesへ再登録した後、実行中Work PagerのNSRunningApplication.iconが生成したページャー画像を返すことを確認した。今後のビルドでも対象バンドルのみ再登録する。起動時にもバンドルのICNSをapplicationIconImageへ明示設定する。Releaseビルドと署名検証成功。
